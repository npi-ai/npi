---
title: NPi AI
---

import Logo from '@components/logo'

<header className="flex flex-col gap-2 items-center justify-center">
  <Logo width={300} />
  <h1 className="text-2xl font-semibold text-slate-500 text-center dark:text-slate-300">Empower AI to Get Things Done for You</h1>
</header>


NPi (**N**atural-language **P**rogramming **I**nterface), pronounced as **"N π"**, is an open-source platform providing **_Tool-use_** APIs to empower AI agents with the ability to operate and interact with a diverse array of software tools and applications.

<nav className="text-center my-4">
  [Getting Started](/docs/getting-started)
  |
  [Examples](/examples)
  |
  [NPi Cloud](https://npiai.dev)
</nav>

## Installation

```sh
pip install npiai
```

## One-Minute Quick Start

Let's create a new tool to compute the nth Fibonacci number. Start by crafting a new Python file titled `main.py` and insert the following snippet:

```py filename="main.py" showLineNumbers
from npiai import FunctionTool, function, agent_wrapper

class MyTool(FunctionTool):
    def __init__(self):
        super().__init__(
            name='my_tool',
            description='test tool',
        )

    @function
    def fibonacci(self, n: int) -> int:
        """
        Get the nth Fibonacci number.

        Args:
            n: The index of the Fibonacci number in the sequence.
        """
        if n == 0:
            return 0
        if n == 1:
            return 1
        return self.fibonacci(n - 1) + self.fibonacci(n - 2)

async def main():
    async with agent_wrapper(MyTool()) as agent:
        result = await agent.chat("What's the 10-th fibonacci number?")
        print(result)

if __name__ == "__main__":
    asyncio.run(main())
```

Now, run the tool:

```sh
python main.py
```

You should see the output `55` in the console. That's it! You've successfully created and run your first NPi tool. 🎉

## Next Steps

- [Read the Documentation](/docs)
- [Explore More Examples](/examples)
- [Join NPi Cloud](https://npiai.dev)
- [Contribute to NPi AI](https://github.com/npi-ai/npi)
