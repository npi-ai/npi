[tool.poetry]
name = "npi"
version = "0.0.1"
description = "Natural language API"
authors = ["wells <wells@npi.ai"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.10"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
